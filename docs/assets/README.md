# Assets

| File | Use |
|------|-----|
| `hero.png` | README banner |
| `demo.gif` | README demo loop |
| `demo.mp4` | Same animation (optional download) |

Regenerate from `scripts/generate-demo-assets.py` plus `ffmpeg` if the visuals need a content update. To change duration only, time-stretch the existing `demo.mp4` and re-export the GIF.
